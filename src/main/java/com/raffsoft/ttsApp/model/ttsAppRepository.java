package com.raffsoft.ttsApp.model;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ttsAppRepository extends JpaRepository<ttsApp,Integer>
{

//	ttsApp findAllByStatus(String status);
//
//	ttsApp findAllByProject(String project);

}
